# autosuggest

verifying my git set-up : )

hi this is verified !
changes flow for pushing 
1. add(plus button)
2. commit with message
3. push / sync 

**commands:**
git add -A
git commit -m "add project"
git push